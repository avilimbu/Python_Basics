# Object Oriented Programmin | Practice Questions

---

1. Create a class `programmer` for storing the information of a few programmer working at microsoft.

2. Write a class `calculator` capable of finding square, cube and square root of a number.

3. Create a class with class attribute a; create a object from it and set 'a' directlu usimg object.a =0. Does this change the class attribute?

4. Add the static method in problem 2, to greet the user with hello.

5. Write a class Train which has methods to book a ticket, get status(no of seats) and get fair information of a train.

6. Can you change the self parameter inside a class to something else (say "rabbit"). Try changing to 'slf' or "rabbit" and see the effect